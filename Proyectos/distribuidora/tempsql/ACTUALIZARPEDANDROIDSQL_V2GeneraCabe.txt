		SELECT id, convert(int,cabepedido.codigocliente) as clientes_numero
		,convert(int,vendedor) as vendedor_codigo,fecha as fecha
		,horain,ISNULL(horaout,'') as horaout
		,chksum,convert(int,vales) as vales
		,convert(int,bonificaciones) as bonificaciones
		,Observaciones,convert(int,TipoDePedido) as tipodepedido
		,fechadeentrega,loteexportacion
		,convert(int,Remito) as remito
		,convert(varchar(200),pedidoid) as pedidoid_pm
		FROM CabPedido as CabePedido
		Where id in (126976)
		order by id