FUNCTION LeerClientes(cArchivo)

CREATE CURSOR CsrLista (deta01 c(250),deta02 c(250),deta03 c(250),deta04 c(250),deta05 c(250),deta06 c(250),deta07 c(250),deta08 c(250) )


Oavisar.proceso('S','Abriendo archivos') 
*stop()

SELECT CsrLista
APPEND FROM  &cArchivo SDF

lcDelimitador = ";"
replace ALL deta01 WITH STRTRAN(deta01,"	",lcDelimitador)
replace ALL deta02 WITH STRTRAN(deta02,"	",lcDelimitador)
replace ALL deta03 WITH STRTRAN(deta03,"	",lcDelimitador)
replace ALL deta04 WITH STRTRAN(deta04,"	",lcDelimitador)
replace ALL deta05 WITH STRTRAN(deta05,"	",lcDelimitador)
replace ALL deta06 WITH STRTRAN(deta06,"	",lcDelimitador)
replace ALL deta07 WITH STRTRAN(deta07,"	",lcDelimitador)
replace ALL deta08 WITH STRTRAN(deta08,"	",lcDelimitador)

Oavisar.proceso('S','Procesando '+alias()) 

cCadeCtacte = "" 


SELECT CsrLista
GO TOP 
*vista()
lnPrimeraOcurrencia = 44
leiunarticulo = .f.
lnMaxCamposLeidos = 8
ldebug = .t.
lnLecturas = 0
SKIP 
*stop()
DO WHILE NOT EOF() AND lnLecturas < 5000000
	lnCantCampo = 39 &&Hay un campo vacio
	lnSiguienteOcurrencia = 1
	lnCamposLeidos = 1 &&Campos de CsrLista
	lcNomCampo = "CsrLista.deta"+strzero(lnCamposLeidos,2)

	IF AT(lcDelimitador,deta01)=1 AND (AT(lcDelimitador,deta01,2)=AT(lcDelimitador,deta01)+1 OR AT(lcDelimitador,deta01,3)=AT(lcDelimitador,deta01,2)+1)
		SKIP 
		LOOP 
	ENDIF 
	
*!*		IF AT(lcDelimitador,deta01)=lnPrimeraOcurrencia
		leiunarticulo = .t.
		STORE "" TO lcAcarreo,lcIdJ
		STORE "" TO lcCodigo,lcCategoria,lcApellido,lcNombre,lcDireccion,LcLocalidad,lcCodPostal,lcProvincia
		STORE "" TO lcTelefono,lcTelefono2,lcFax,lcCelular,lcEmail,lcfecAlta,lcTipoDoc,lcDocumento,lcCuit
		STORE "" TO lcTipoIVA,lcVendedor,lcZona,lcCodVendedor,lcDireNro,lcDirePiso,lcDireDpto,lcLista
		STORE "" TO lcEstado,lcCodLista,lcCodCateIVA,lcCodProvincia	,lcCodLocalidad,lcLista,lcOcupacion
		STORE "" TO lcFecNac, lcSexo,lcCodPostal
		
		j = 0
*!*		ELSE
*!*			IF !leiunarticulo
*!*				SKIP 
*!*				LOOP 
*!*			ENDIF 
*!*		ENDIF 
	
	DO WHILE lnCamposLeidos<=lnMaxCamposLeidos 
		i = 1
		DO WHILE i + j <= lnCantCampo &&Campos de CsrArti + 1
			lnpos = AT(lcDelimitador,&lcNomCampo,i)
			IF lnPos#0 &&No es fin de linea
				lccadena = ALLTRIM(lcAcarreo) + SUBSTR(&lcNomCampo,lnSiguienteOcurrencia,lnpos-(lnSiguienteOcurrencia))
				lcAcarreo = ""
			ELSE 
				lcAcarreo = ALLTRIM(lcAcarreo) + ALLTRIM(SUBSTR(&lcNomCampo,lnSiguienteOcurrencia))
				EXIT 
			ENDIF
			*lcIdJ			= UPPER(LimpiarCadena(IIF(j + i=1,lcCadena,lcIdJ)))
			lcCodigo		= UPPER(LimpiarCadena(IIF(j + i=1,lcCadena,lcCodigo)))
			lcApellido		= UPPER(LimpiarCadena(IIF(j + i=2,lcCadena,lcApellido)))
			lcNombre		= UPPER((IIF(j + i=3,lcCadena,lcNombre)))
			lcDocumento	= UPPER(LimpiarCadena(IIF(j + i=24,lcCadena,lcDocumento)))
			lcTipoDoc		= UPPER(LimpiarCadena(IIF(j + i=4,lcCadena,lcTipoDoc)))
			lcCuit		= UPPER(LimpiarCadena(IIF(j + i=7,lcCadena,lcCuit)))
			lcDireccion	= UPPER(LimpiarCadena(IIF(j + i=8,lcCadena,lcDireccion)))
			lcDireNro		= UPPER(LimpiarCadena(IIF(j + i=9,lcCadena,lcDireNro)))
			lcDirePiso		= UPPER(LimpiarCadena(IIF(j + i=10,lcCadena,lcDirePiso)))
			lcDireDpto	= UPPER(LimpiarCadena(IIF(j + i=11,lcCadena,lcDireDpto)))
			
			LcCodLocalidad	= UPPER(LimpiarCadena(IIF(j + i=12,lcCadena,lcCodLocalidad)))
			LcLocalidad		= UPPER(LimpiarCadena(IIF(j + i=13,lcCadena,lcLocalidad)))
			lcCodPostal		= UPPER(LimpiarCadena(IIF(j + i=12,lcCadena,lcCodPostal)))
			
			lcCodProvincia	= UPPER(LimpiarCadena(IIF(j + i=15,lcCadena,lcCodProvincia)))
			lcOcupacion	= UPPER(LimpiarCadena(IIF(j + i=25,lcCadena,lcOcupacion)))
			lcTipoIVA		= UPPER(LimpiarCadena(IIF(j + i=5,lcCadena,lcTipoIVA)))
			lcSexo		= UPPER(LimpiarCadena(IIF(j + i=31,lcCadena,lcSexo)))
			lcFecNac		= UPPER(LimpiarCadena(IIF(j + i=30,lcCadena,LEFT(lcFecNac,8))))
			*lcCategoria		= UPPER(LimpiarCadena(IIF(j + i=13,lcCadena,lcCategoria)))
			*lcCodLista		= UPPER(LimpiarCadena(IIF(j + i=14,lcCadena,lcCodLista))) 
			*lcLista			= UPPER(LimpiarCadena(IIF(j + i=15,lcCadena,lcLista))) 
			*lcCodVendedor	= UPPER(LimpiarCadena(IIF(j + i=16,lcCadena,lcCodVendedor)))
			*lcVendedor		= UPPER(LimpiarCadena(IIF(j + i=17,lcCadena,lcVendedor)))			
			lcTelefono		= UPPER(LimpiarCadena(IIF(j + i=17,lcCadena,lcTelefono)))			
			*lcCelular		= UPPER(LimpiarCadena(IIF(j + i=19,lcCadena,lcCelular)))
			*lcEstado		= UPPER(LimpiarCadena(IIF(j + i=20,lcCadena,lcEstado)))
			*lcZona			= UPPER(LimpiarCadena(IIF(j + i=21,lcCadena,lcZona)))
			lcEmail			= UPPER(LimpiarCadena(IIF(j + i=19,lcCadena,lcEmail)))
			
			*lcFax			= UPPER(LimpiarCadena(IIF(j + i=12,lcCadena,lcFax)))			
			*lcfecAlta		= IIF(j + i=19,lcCadena,lcFecAlta)
			
							
			lnSiguienteOcurrencia = lnPos + 1
			i = i + 1
			
			IF VAL(lcCodigo)=1772 and ldebug
				*stop()
				ldebug = .f.
			ENDIF 
		
		ENDDO 
		lnSiguienteOcurrencia = 1
		lnCamposLeidos = lnCamposLeidos + 1
		lcNomCampo = "CsrLista.deta"+strzero(lnCamposLeidos,2)
		IF lnPos = 0 AND i <= lnCantCampo &&Si no termino, y no es un campo csrati q nop existe
			 j = j + (i - 1)
		ENDIF 
		IF lnpos#0 AND i+j >= lnCantCampo
			EXIT 
		ENDIF 
	ENDDO 

	IF lnpos#0 AND i+j >= lnCantCampo
		&&Insertamos si se encontro una ultima ocurrencia con respecto a la cantidad de registros
		&&Que se grabaran en csrarti.
		&&Esta diseñado para leer hasta los precios.
		&&Si se quiere leer todo. Se necesita un caracter de finalizado de linea.
		
		IF ASC(LEFT(lcNombre,1))=149 OR ASC(LEFT(lcNombre,1))=149 OR lentrim(lcNombre)=0 OR LEFT(lcNombre,3)='---'
			SKIP 
			LOOP 
			
		ENDIF 
		IF '*'$lcTelefono
			*LOOP
		ENDIF 
		*lcCodigo = SUBSTR(lcCodigo,4)
		
		INSERT INTO CsrDeudor (Codigo,Categoria,Nombre,Direccion,Localidad,CodPostal,Provincia;
		,Telefono,Telefono2,Fax,Celular,Email,fecAlta,TipoDoc,Documento;
		,TipoIVA,Vendedor,Zona,ctadeudor,DireNro,DirePiso,DireDpto,Lista,Estado,CodLista;
		,CodCateIVA,CodLocalidad,CodProvincia,CodVendedor,idorigen;
		,Cuit, Ocupacion, FecNac, Sexo, Apellido) ;
		values (lcCodigo,lcCategoria,lcNombre,lcDireccion,LcLocalidad,lcCodPostal,lcProvincia ;
		,lcTelefono,lcTelefono2,lcFax,lcCelular,lcEmail,lcfecAlta,lcTipoDoc,lcDocumento ;
		,lcTipoIVA,lcVendedor,lcZona,1,lcDireNro,lcDirePiso,lcDireDpto,lcLista,lcEstado,VAL(lcCodLista);
		,VAL(lcCodCateIVA),lcCodLocalidad,lcCodProvincia,lcCodVendedor,VAL(lcIdJ);
		,lcCuit, lcOcupacion, lcFecNac, lcSexo, lcApellido)
				
		*replace descripcion WITH lmDescripcion IN FsrArticulo
		leiunarticulo = .f.
		lnLecturas = lnLecturas + 1 
	ENDIF 
	SKIP IN CsrLista
ENDDO 


USE IN CsrLista

ENDFUNC 

FUNCTION LeerEmpresas(cArchivo)

CREATE CURSOR CsrLista (deta01 c(250),deta02 c(250),deta03 c(250),deta04 c(250),deta05 c(250),deta06 c(250),deta07 c(250),deta08 c(250) )

	
Oavisar.proceso('S','Abriendo archivos') 
*stop()

SELECT CsrLista
APPEND FROM  &cArchivo SDF

lcDelimitador = ";"
replace ALL deta01 WITH STRTRAN(deta01,"	",lcDelimitador)
replace ALL deta02 WITH STRTRAN(deta02,"	",lcDelimitador)
replace ALL deta03 WITH STRTRAN(deta03,"	",lcDelimitador)
replace ALL deta04 WITH STRTRAN(deta04,"	",lcDelimitador)
replace ALL deta05 WITH STRTRAN(deta05,"	",lcDelimitador)
replace ALL deta06 WITH STRTRAN(deta06,"	",lcDelimitador)
replace ALL deta07 WITH STRTRAN(deta07,"	",lcDelimitador)
replace ALL deta08 WITH STRTRAN(deta08,"	",lcDelimitador)

Oavisar.proceso('S','Procesando '+alias()) 

cCadeCtacte = "" 


SELECT CsrLista
GO TOP 
*vista()
lnPrimeraOcurrencia = 44
leiunarticulo = .f.
lnMaxCamposLeidos = 8
ldebug = .t.
lnLecturas = 0
SKIP 
*stop()
DO WHILE NOT EOF() AND lnLecturas < 5000000
	lnCantCampo = 17 &&Hay un campo vacio
	lnSiguienteOcurrencia = 1
	lnCamposLeidos = 1 &&Campos de CsrLista
	lcNomCampo = "CsrLista.deta"+strzero(lnCamposLeidos,2)

	IF AT(lcDelimitador,deta01)=1 AND (AT(lcDelimitador,deta01,2)=AT(lcDelimitador,deta01)+1 OR AT(lcDelimitador,deta01,3)=AT(lcDelimitador,deta01,2)+1)
		SKIP 
		LOOP 
	ENDIF 
	
*!*		IF AT(lcDelimitador,deta01)=lnPrimeraOcurrencia
		leiunarticulo = .t.
		STORE "" TO lcAcarreo,lcIdJ
		STORE "" TO lcCodigo,lcCategoria,lcApellido,lcNombre,lcDireccion,LcLocalidad,lcCodPostal,lcProvincia
		STORE "" TO lcTelefono,lcTelefono2,lcFax,lcCelular,lcEmail,lcfecAlta,lcTipoDoc,lcDocumento,lcCuit
		STORE "" TO lcTipoIVA,lcVendedor,lcZona,lcCodVendedor,lcDireNro,lcDirePiso,lcDireDpto,lcLista
		STORE "" TO lcEstado,lcCodLista,lcCodCateIVA,lcCodProvincia	,lcCodLocalidad,lcLista,lcOcupacion
		STORE "" TO lcFecNac, lcSexo,lcCodPostal,lcCodPais
		
		j = 0
*!*		ELSE
*!*			IF !leiunarticulo
*!*				SKIP 
*!*				LOOP 
*!*			ENDIF 
*!*		ENDIF 
	
	DO WHILE lnCamposLeidos<=lnMaxCamposLeidos 
		i = 1
		DO WHILE i + j <= lnCantCampo &&Campos de CsrArti + 1
			lnpos = AT(lcDelimitador,&lcNomCampo,i)
			IF lnPos#0 &&No es fin de linea
				lccadena = ALLTRIM(lcAcarreo) + SUBSTR(&lcNomCampo,lnSiguienteOcurrencia,lnpos-(lnSiguienteOcurrencia))
				lcAcarreo = ""
			ELSE 
				lcAcarreo = ALLTRIM(lcAcarreo) + ALLTRIM(SUBSTR(&lcNomCampo,lnSiguienteOcurrencia))
				EXIT 
			ENDIF
			*lcIdJ			= UPPER(LimpiarCadena(IIF(j + i=1,lcCadena,lcIdJ)))
			lcCodigo		= UPPER(LimpiarCadena(IIF(j + i=1,lcCadena,lcCodigo)))
			lcNombre		= UPPER(LimpiarCadena(IIF(j + i=3,lcCadena,lcNombre)))
			*lcDocumento	= UPPER(LimpiarCadena(IIF(j + i=24,lcCadena,lcDocumento)))
			*lcTipoDoc		= UPPER(LimpiarCadena(IIF(j + i=4,lcCadena,lcTipoDoc)))
			lcCuit		= UPPER(LimpiarCadena(IIF(j + i=6,lcCadena,lcCuit)))
			lcDireccion	= UPPER(LimpiarCadena(IIF(j + i=7,lcCadena,lcDireccion)))
			lcDireNro		= UPPER(LimpiarCadena(IIF(j + i=8,lcCadena,lcDireNro)))
			lcDirePiso		= UPPER(LimpiarCadena(IIF(j + i=9,lcCadena,lcDirePiso)))
			lcDireDpto	= UPPER(LimpiarCadena(IIF(j + i=10,lcCadena,lcDireDpto)))
			
			LcCodLocalidad	= UPPER(LimpiarCadena(IIF(j + i=11,lcCadena,lcCodLocalidad)))
			LcLocalidad		= UPPER(LimpiarCadena(IIF(j + i=12,lcCadena,lcLocalidad)))
			lcCodPostal		= UPPER(LimpiarCadena(IIF(j + i=11,lcCadena,lcCodPostal)))
			
			lcCodProvincia	= UPPER(LimpiarCadena(IIF(j + i=13,lcCadena,lcCodProvincia)))
			lcCodPais		= UPPER(LimpiarCadena(IIF(j + i=14,lcCadena,lcCodPais)))
			
			*lcOcupacion	= UPPER(LimpiarCadena(IIF(j + i=25,lcCadena,lcOcupacion)))
			lcTipoIVA		= UPPER(LimpiarCadena(IIF(j + i=5,lcCadena,lcTipoIVA)))
			*lcSexo		= UPPER(LimpiarCadena(IIF(j + i=31,lcCadena,lcSexo)))
			*lcFecNac		= UPPER(LimpiarCadena(IIF(j + i=30,lcCadena,LEFT(lcFecNac,8))))
			*lcCategoria		= UPPER(LimpiarCadena(IIF(j + i=13,lcCadena,lcCategoria)))
			*lcCodLista		= UPPER(LimpiarCadena(IIF(j + i=14,lcCadena,lcCodLista))) 
			*lcLista			= UPPER(LimpiarCadena(IIF(j + i=15,lcCadena,lcLista))) 
			*lcCodVendedor	= UPPER(LimpiarCadena(IIF(j + i=16,lcCadena,lcCodVendedor)))
			*lcVendedor		= UPPER(LimpiarCadena(IIF(j + i=17,lcCadena,lcVendedor)))			
			lcTelefono		= UPPER(LimpiarCadena(IIF(j + i=15,lcCadena,lcTelefono)))			
			*lcCelular		= UPPER(LimpiarCadena(IIF(j + i=19,lcCadena,lcCelular)))
			*lcEstado		= UPPER(LimpiarCadena(IIF(j + i=20,lcCadena,lcEstado)))
			*lcZona			= UPPER(LimpiarCadena(IIF(j + i=21,lcCadena,lcZona)))
			lcEmail			= UPPER(LimpiarCadena(IIF(j + i=17,lcCadena,lcEmail)))
			
			*lcFax			= UPPER(LimpiarCadena(IIF(j + i=12,lcCadena,lcFax)))			
			*lcfecAlta		= IIF(j + i=19,lcCadena,lcFecAlta)
			
							
			lnSiguienteOcurrencia = lnPos + 1
			i = i + 1
			
			IF VAL(lcCodigo)=1772 and ldebug
				*stop()
				ldebug = .f.
			ENDIF 
		
		ENDDO 
		lnSiguienteOcurrencia = 1
		lnCamposLeidos = lnCamposLeidos + 1
		lcNomCampo = "CsrLista.deta"+strzero(lnCamposLeidos,2)
		IF lnPos = 0 AND i <= lnCantCampo &&Si no termino, y no es un campo csrati q nop existe
			 j = j + (i - 1)
		ENDIF 
		IF lnpos#0 AND i+j >= lnCantCampo
			EXIT 
		ENDIF 
	ENDDO 

	IF lnpos#0 AND i+j >= lnCantCampo
		&&Insertamos si se encontro una ultima ocurrencia con respecto a la cantidad de registros
		&&Que se grabaran en csrarti.
		&&Esta diseñado para leer hasta los precios.
		&&Si se quiere leer todo. Se necesita un caracter de finalizado de linea.
		
		IF ASC(LEFT(lcNombre,1))=149 OR ASC(LEFT(lcNombre,1))=149 OR lentrim(lcNombre)=0 OR LEFT(lcNombre,3)='---'
			SKIP 
			LOOP 
			
		ENDIF 
		IF '*'$lcTelefono
			*LOOP
		ENDIF 
		*lcCodigo = SUBSTR(lcCodigo,4)
		
		INSERT INTO CsrDeudor (Codigo,Categoria,Nombre,Direccion,Localidad,CodPostal,Provincia;
		,Telefono,Telefono2,Fax,Celular,Email,fecAlta,TipoDoc,Documento;
		,TipoIVA,Vendedor,Zona,ctadeudor,DireNro,DirePiso,DireDpto,Lista,Estado,CodLista;
		,CodCateIVA,CodLocalidad,CodProvincia,CodVendedor,idorigen;
		,Cuit, Ocupacion, FecNac, Sexo, Apellido) ;
		values (lcCodigo,lcCategoria,lcNombre,lcDireccion,LcLocalidad,lcCodPostal,lcProvincia ;
		,lcTelefono,lcTelefono2,lcFax,lcCelular,lcEmail,lcfecAlta,lcTipoDoc,lcDocumento ;
		,lcTipoIVA,lcVendedor,lcZona,1,lcDireNro,lcDirePiso,lcDireDpto,lcLista,lcEstado,VAL(lcCodLista);
		,VAL(lcCodCateIVA),lcCodLocalidad,lcCodProvincia,lcCodVendedor,VAL(lcIdJ);
		,lcCuit, lcOcupacion, lcFecNac, lcSexo, lcApellido)
				
		*replace descripcion WITH lmDescripcion IN FsrArticulo
		leiunarticulo = .f.
		lnLecturas = lnLecturas + 1 
	ENDIF 
	SKIP IN CsrLista
ENDDO 


USE IN CsrLista

ENDFUNC 